# go_cinema_system

Небольшой HTTP-сервис на Go.

## Требования

- Go 1.22+

## Запуск

Из корня проекта:

```bash
go run ./cmd/app
```

Сервер стартует на `:8080`.

Примеры:

- `GET /`
- `GET /test`
- `GET /slow` (ответ с задержкой)
